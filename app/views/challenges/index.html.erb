<div class="container">
  <% @challenges.each do |challenge| %>
  <div class="card-challenge">
    <div class="card-c-infos">
      <div class="card-c-details">
        <p>Category: <%= challenge.category %></p>
        <h1>Distance: <%= challenge.distance / 100 %> km</h1>
      </div>
      <div class="card-c-bet">
        <h2><%= challenge.bet %> €</h2>
      </div>
    </div>
    <div class="btn-flat text-center mt-3" >
      <%= link_to "Bet & Run now !", challenge_runs_path(challenge), method: :post, class: "btn-flat"  %>
    </div>
  </div>

  <% end %>
</div>
